 Named Entities:
    For each provided Concept(Corresponding Named Entity,..),
    Method(Customized CNN),
    RawData(Images),
    DataFormat(Image),
    DataAnnotationTechnique(Bounding Box),
    DataAugmentationTechnique(Flipping, Rotating, Scaling, Shearing),
    Dataset(Created by selecting images of different individual insects from the captured sequences of images in the period of observation),
    PreprocessingStep(Resizing images to 128 × 128 pixels and scaling pixel values to a range of 0 to 1),
    DataSplitCriteria(80% for training and 20% for validation),
    CodeRepository(Not mentioned),
    DataRepository(Not mentioned),
    CodeRepositoryLink(Not mentioned),
    DataRepositoryLink(Not mentioned),
    DeepLearningModel(Customized CNN),
    Hyperparameter(Kernel size, Convolutional depth, Fully connected size, Optimizer),
    HyperparameterOptimization(Not mentioned),
    OptimizationTechnique(Adaptive Moment Estimation (Adam), Stochastic Gradient Decent (SGD)),
    TrainingCompletionCriteria(Not mentioned),
    RegularizationMethod(Dropout),
    ModelPerformanceMonitoringStrategy(F1-score),
    Framework(Not mentioned),
    HardwareResource(Not mentioned),
    PostprocessingStep(Not mentioned),
    PerformanceMetric(F1-score),
    GeneralizabilityMeasure(Not mentioned),
    RandomnessStrategy(Not mentioned),
    ModelPurpose(Species classification),
    DataBiasTechnique(Not mentioned),
    ModelDeploymentProcess(Not mentioned),
    DeploymentPlatform(Not mentioned).