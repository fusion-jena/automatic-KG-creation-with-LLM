 Named Entities:
    For each provided Concept(Corresponding Named Entity,..),
    Method(Self-supervised RGB deep learning model),
    RawData(RGB data, LIDAR data),
    DataFormat(Image data, Point cloud data),
    DataAnnotationTechnique(Hand-annotated bounding boxes),
    Dataset(Not explicitly mentioned),
    PreprocessingStep(Not explicitly mentioned),
    DataSplitCriteria(Not explicitly mentioned),
    CodeRepository(Not explicitly mentioned),
    DataRepository(Not explicitly mentioned),
    CodeRepositoryLink(Not explicitly mentioned),
    DataRepositoryLink(Not explicitly mentioned),
    DeepLearningModel(Self-supervised RGB deep learning model),
    Hyperparameter(Not explicitly mentioned),
    HyperparameterOptimization(Not explicitly mentioned),
    OptimizationTechnique(Not explicitly mentioned),
    TrainingCompletionCriteria(Not explicitly mentioned),
    RegularizationMethod(Not explicitly mentioned),
    ModelPerformanceMonitoringStrategy(Not explicitly mentioned),
    Framework(Not explicitly mentioned),
    HardwareResource(Not explicitly mentioned),
    PostprocessingStep(Not explicitly mentioned),
    PerformanceMetric(Stem recall, Precision, Recall),
    GeneralizabilityMeasure(Not explicitly mentioned),
    RandomnessStrategy(Not explicitly mentioned),
    ModelPurpose(Tree delineation),
    DataBiasTechnique(Not explicitly mentioned),
    ModelDeploymentProcess(Not explicitly mentioned),
    DeploymentPlatform(Not explicitly mentioned).