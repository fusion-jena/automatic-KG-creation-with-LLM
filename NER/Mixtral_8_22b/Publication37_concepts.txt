 Named Entities:
    For each provided Concept(Corresponding Named Entity,..),
    Method(Deep Learning Pipeline),
    RawData(Audio Data),
    DataFormat(Audio),
    Dataset(CLO-43SD),
    PreprocessingStep(Normalization, Scaling, Cleaning),
    DataAugmentationTechnique(Adding Background Noise, Dynamic Range Compression, Pitch Shifting, Time Stretching),
    DeepLearningModel(Deep Convolutional Neural Network),
    Hyperparameter(Learning Rate, Dropout, L2-Regularization),
    HyperparameterOptimization(Cross-Entropy Loss, Mini-Batch Stochastic Gradient Descent),
    OptimizationTechnique(Stochastic Gradient Descent),
    TrainingCompletionCriteria(100 Epochs, Checkpointing After Each Epoch, Validation Set),
    RegularizationMethod(Dropout, L2-Regularization),
    ModelPerformanceMonitoringStrategy(Classification Accuracy, 5-Fold Cross Validation),
    Framework(Lasagne, Theano),
    ModelPurpose(Classification),
    DataBiasTechnique(Not Mentioned),
    ModelDeploymentProcess(Not Mentioned),
    DeploymentPlatform(Not Mentioned).