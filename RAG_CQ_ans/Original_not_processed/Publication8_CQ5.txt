 The deep learning pipeline in this study uses the ImageNet dataset for transfer learning. The
initial layers of the CNN are initialized with pre-trained weights from the ImageNet dataset, and
the deeper layers are fine-tuned using the annotated herbarium scan dataset.