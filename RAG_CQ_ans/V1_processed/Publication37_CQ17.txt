The deep learning pipeline uses dropout and L2 regularization to prevent overfitting. Dropout is applied to the input of the last two layers with probability 0. 5, and L2-regularization is applied to the weights of the last two layers with a penalty factor of 0. 001. 