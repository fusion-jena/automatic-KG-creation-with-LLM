The annotations or labels are used as the target variable in the model to predict species richness values for each grid cell. The model learns the relationship between the input variables and the target variable to make predictions for new, unseen data.