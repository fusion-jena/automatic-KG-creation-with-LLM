The model weights are updated by retraining with new data every time the model is trained. In this study, the model was trained on the MNHN Paris Herbarium dataset and then retrained on the Herbarium Senckenbergianum dataset.